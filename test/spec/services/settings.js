'use strict';

/*describe('Service: Settings', function () {

  // load the service's module
  beforeEach(module('crowdNewsApp.services'));

  // instantiate service
  var Settings;
  beforeEach(inject(function (_Settings_) {
    Settings = _Settings_;
  }));

  it('should do something', function () {
    expect(!!Settings).toBe(true);
  });

  it('should be able to set the subreddit', function() {
    Settings.setSubreddit('cars');
    expect(Settings.getSubreddit()).toEqual('cars');
  });

});*/
